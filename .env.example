# Copy this file to .env and fill in your values
# cp .env.example .env

# Sentry DSN (optional — Sentry is disabled if not set)
# Get yours at https://sentry.io → Settings → Projects → Client Keys
SENTRY_DSN=

# These are set automatically by docker-compose.yml
# Only override if running services outside Docker
# DATABASE_URL=postgresql://signal:signal@localhost:5432/signallab
# LOKI_URL=http://localhost:3100
